<%= link_to "/favorites/#{@app.id}/create", class: 'btn favorite-btn', method: :post, remote: true do %><%= data: { confirm: '本当にこの投稿を削除して良いですか？' }  %>
  <span><i class="far fa-star"></i> お気に入り追加</span>
<% end %>